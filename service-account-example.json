/* 
See https://firebase.google.com/docs/admin/setup for walkthrough
This app will not work without a valid service worker and firebase realtime database

Remove these comments and replace below with your credentials, and rename file, 
Or simply add your own service-account-key.json from firebase 
*/

{
  "type": "service_account",
  "project_id": "your-credentials",
  "private_key_id": "your-credentials",
  "private_key": "your-credentials",
  "client_email": "your-credentials",
  "client_id": "your-credentials",
  "auth_uri": "https://accounts.google.com/o/oauth2/auth",
  "token_uri": "https://oauth2.googleapis.com/token",
  "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
  "client_x509_cert_url": "your-credentials"
}
